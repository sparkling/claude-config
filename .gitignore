# ─── Claude Code runtime state (never commit) ───
history.jsonl
.credentials.json
stats-cache.json
*.claude.json

# Ephemeral directories (recreated each session)
session-env/
debug/
file-history/
shell-snapshots/
todos/
tasks/
teams/
plans/
cache/
paste-cache/
ide/

# Telemetry & analytics
telemetry/
statsig/

# Internal project state (per-project data in global config)
projects/

# Plugins (local installs, marketplace caches)
plugins/cache/
plugins/blocklist.json
plugins/config.json
plugins/install-counts-cache.json
plugins/installed_plugins.json
plugins/known_marketplaces.json
plugins/marketplaces/

# Settings (managed by Claude Code, not user config)
settings.json

# Backups
backups/

# Dependencies
node_modules/
package-lock.json

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Temp
*.tmp
*.temp

# Stale leftovers
statusline-command.sh
pligins.backup/
downloads/
lessons/

# Runtime state (per-machine; not config)
daemon/
sessions/
jobs/
chrome/

# Plugin runtime/caches (per-machine)
plugins/data/
plugins/.last_inuse_sweep
plugins/plugin-catalog-cache.json

# One-off caches / markers
.last-cleanup
.last-update-result.json
mcp-needs-auth-cache.json

# Backup files
*.bak
*.bak-*
*.backup.*
